# このプロジェクトの編集ルール

- `cheat_sheet.py`、`python_cheat_sheet.md`、`README.md` の説明コメントは、コード行の右側ではなく、対応するコードの上か下に独立した行で配置する。
- 説明コメントと対応するコードを1セットとして扱い、セット間には空行を1行入れる。説明コメントと対応するコードの間には空行を入れない。
- この空行のルールは、このプロジェクトにのみ適用する。

```python
# 最大値
max(numbers)

# 最小値
min(numbers)
```
